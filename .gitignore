# Node / SvelteKit
node_modules/
build/
.svelte-kit/
*.log

# Rust (target is C:\cargo-targets, but ignore local target/ too)
src-tauri/target/
target/

# Built exe (reproducible from build; canonical output is C:\cargo-targets)
bin/

# Env / secrets
.env
.env.*
.secrets/

# OS
Thumbs.db
.DS_Store

# Handoff (per workspace convention — gitignored)
HANDOFF.md

# Claude Code local runtime state
.claude/

# Internal working docs — local only
/CLAUDE.md
